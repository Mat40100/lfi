# Secrets — never commit
.env
dog/dog.env
console/console.env

# IDE
.idea/

# Persisted runtime data (Ghost content, MySQL, Caddy state)
data/

# Server-side backups (Caddyfile/.env/compose snapshots)
*.bak.*
*.bak-*
